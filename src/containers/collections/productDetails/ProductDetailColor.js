import React from 'react';

function ProductDetailColor({ color }) {
  return (
    <div style={{ backgroundColor: `${color}` }} className="dot-div1"></div>
  );
}

export default ProductDetailColor;
